- article_opts = @page.draft? ? { class: 'draft' } : {}

%nav.breadcrumb
  - display_breadcrumbs

#content
  %article{ article_opts }
    ~ @page.to_html(self)
    = haml :page_meta, :layout => false, :locals => { :page => @page }    